services:
  postgres:
    image: postgres:16-alpine
    container_name: praxis-postgres
    environment:
      POSTGRES_USER: praxis
      POSTGRES_PASSWORD: praxis
      POSTGRES_DB: praxis
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U praxis -d praxis"]
      interval: 5s
      timeout: 5s
      retries: 5

  api-gateway:
    build:
      context: .
      dockerfile: apps/api_gateway/Dockerfile
    container_name: praxis-api-gateway
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://praxis:praxis@postgres:5432/praxis
      SECRET_KEY: ${SECRET_KEY:-change-me-in-production}
      ENV: production
      DEBUG: "false"
      ALLOWED_ORIGINS: "http://localhost:3000"
      DECISION_SERVICE_URL: http://decision-service:8001
      PLATFORM_SERVICE_URL: http://platform-service:8080
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  decision-service:
    build:
      context: .
      dockerfile: services/decision-service/Dockerfile
    container_name: praxis-decision-service
    ports:
      - "8001:8001"
    environment:
      DATABASE_URL: postgresql://praxis:praxis@postgres:5432/praxis
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  platform-service:
    build:
      context: .
      dockerfile: services/platform-service/Dockerfile
    container_name: praxis-platform-service
    ports:
      - "8080:8080"
    environment:
      DATABASE_URL: postgresql://praxis:praxis@postgres:5432/praxis
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  web:
    build:
      context: ./apps/web
      dockerfile: Dockerfile
    container_name: praxis-web
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8000
    depends_on:
      - api-gateway

volumes:
  postgres_data:
