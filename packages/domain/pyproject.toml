[project]
name = "aether-domain"
version = "0.1.0"
description = "Domain models and policies for Aether Sentinel"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["domain"]
