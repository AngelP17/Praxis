[project]
name = "aether-pipelines"
version = "0.1.0"
description = "Ingestion and processing pipelines for Aether Sentinel"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.2.0",
    "openpyxl>=3.1.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pipelines"]
