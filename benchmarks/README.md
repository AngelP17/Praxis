# Praxis Benchmarks

Praxis benchmarks validate that each solution pack can produce a proof object, deterministic replay metadata, evidence trust score, ontology confidence, value case, and executive-readout-ready artifact.

Run:

```bash
make praxis-benchmark
```
