# Data Flow

Customer signals enter as solution-pack events, are normalized into operational ontology objects, evaluated by deterministic decision logic, logged as human actions, and emitted as proof and readout artifacts.
